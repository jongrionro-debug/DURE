# Dure Context

이 문서는 Dure 코드베이스를 읽을 때 사용하는 도메인 언어를 고정한다.

## Product Goal

Dure는 기관 운영자가 마을, 사업, 수업, 강사, 참여자를 관리하고, 수업 세션별 출석과 교육일지를 강사에게 제출받는 local-first MVP다.

1차 목표는 배포보다 로컬에서 핵심 운영 흐름이 실제로 동작하는 것이다.

## Domain Terms

### 기관

운영 단위다. 코드에서는 `organizations`로 저장된다.

기관에 속한 데이터는 반드시 `organization_id`로 분리된다. 이 프로젝트에서 기관 범위 누락은 가장 위험한 버그 중 하나다.

### 멤버십

사용자와 기관의 소속 관계다. 코드에서는 `organization_memberships`로 저장된다.

MVP에서는 사용자 한 명이 하나의 기관에만 속한다. 승인되지 않은 멤버십은 업무 화면에 접근할 수 없다.

### 역할

기관 안에서 사용자가 할 수 있는 일을 정한다.

- `organization_admin`: 기관 운영자. 설정, 사용자 관리, 세션 생성, 기록 조회를 수행한다.
- `teacher`: 강사. 배정된 세션을 보고 출석과 교육일지를 제출한다.
- `platform_admin`: 초기 지원용 역할. MVP에서는 별도 복잡한 UI가 필수는 아니다.

### 마을

기관 안의 지역 또는 운영 단위다. 코드에서는 `villages`로 저장된다.

기관 생성 온보딩에서는 첫 마을을 같이 만든다.

### 사업

기관이 운영하는 예산, 보고, 운영상의 상위 묶음이다. 사용자 화면에서는 `사업`으로 보이고, 코드 내부에서는 기존 `programs`, `programId`를 유지한다.

### 프로그램

사업 아래에서 실제로 진행되는 교육 내용 단위다. 사용자 화면에서는 `프로그램`으로 부르고, 코드에서는 기존 `classes`, `classId`를 유지한다.

강사 배정과 참여자 명단의 기준이 된다.

프로그램은 마을에 종속되지 않는다. 마을은 프로그램이 실제로 열리는 수업 일정에서 선택한다.

_Avoid_: 사용자 화면에서 `수업`이라는 용어를 새 생성 흐름의 주 용어로 쓰지 않는다.

### 강사 배정

강사를 특정 수업에 연결하는 관계다. 코드에서는 `teacher_assignments`로 저장된다.

세션 생성 시 강사를 지정하려면 해당 강사가 그 수업에 배정되어 있어야 한다.

### 참여자

마을에 속하는 학습자 또는 출석 대상자다. 코드에서는 기존 `participants`로 저장된다.

수업 일정을 만들 때 선택한 마을의 참여자가 기본 출석 후보로 올라오고, 운영자는 그중 일부를 제외할 수 있다.

_Avoid_: 참여자를 프로그램 자체에만 묶어서 여러 마을의 명단이 섞이게 하지 않는다.

### 수업 일정

특정 날짜에 진행되는 프로그램의 한 회차다. 사용자 화면에서는 `수업 일정`으로 부르고, 코드에서는 기존 `sessions`, `sessionId`를 유지한다.

운영자는 강사나 참여자 명단이 아직 없어도 수업 일정을 먼저 만들 수 있다. 이 create-first 흐름은 Dure의 핵심 운영 방식이다.

강사 배정은 선택 사항이다. 강사가 없는 수업 일정은 운영자 화면에서 `강사 미배정` 상태로 명확히 보여야 한다.

사용자 화면의 생성 흐름에서는 기존 사업, 마을, 프로그램을 선택하거나 즉석으로 만들 수 있어야 한다. 별도의 사업 생성, 마을 생성, 프로그램 생성 화면을 먼저 거치게 하지 않는다.

수업 일정 생성 중 입력한 새 사업, 마을, 프로그램 이름이 기관 안에 이미 있으면 새로 만들지 않고 기존 항목을 재사용한다.

수업 일정 생성의 필수 입력은 진행일, 사업, 마을, 프로그램이다. 강사, 참여자 제외, 메모는 선택 입력이다.

수업 일정을 만들면 운영자는 방금 만든 수업 일정의 상세 관리 화면으로 이동한다. 상세 관리 화면에서 자동으로 올라온 출석 대상자를 제외하거나, 강사를 배정하거나, 기록 상태를 확인한다.

설정 화면의 사업, 마을, 프로그램 생성 폼은 마스터 데이터 정리용으로 남긴다. 실제 운영의 주 생성 동선은 대시보드의 수업 일정 만들기다.

_Avoid_: 사용자 화면에서 `세션`이라는 용어를 쓰지 않는다.

### 세션 참여자 스냅샷

수업 일정 생성 시점에 고정되는 출석 대상자 목록이다. 코드에서는 `session_participant_snapshots`로 저장된다.

참여자 원본이 나중에 바뀌어도 기존 수업 일정의 출석 기준은 자동으로 바뀌지 않는다.

운영자 화면은 수업 일정 생성 시 "현재 마을 참여자 명단을 출석 대상으로 고정한다"는 점을 명시해야 한다.

### 출석 기록

세션 참여자 스냅샷별 출석 상태다. 코드에서는 `attendance_records`로 저장된다.

상태는 `present`, `absent`, `late`, `excused` 중 하나다.

### 교육일지

강사가 세션에 대해 작성하는 텍스트 기록이다. 코드에서는 `lesson_journals`로 저장된다.

### 첨부

강사 제출에 붙는 파일 metadata다. 코드에서는 `attachments`로 저장되고, 실제 파일은 Supabase Storage에 저장된다.

### 제출 시각

`submitted_at`은 최초 제출 시각이다. 이후 수정해도 최초 제출 시각은 유지한다.

### 수정 시각

`updated_at`은 최근 수정 시각이다. 운영자 대시보드에서는 최근 제출 기록과 최근 수정 기록을 분리해서 해석한다.

## Main Workflows

### 기관 생성 온보딩

회원가입 후 기관이 없는 사용자는 기관 이름과 첫 마을 이름을 입력한다. 시스템은 기관, 첫 마을, 첫 운영자 멤버십을 만든다.

### 운영 기본정보 설정

운영자는 마을, 사업, 수업, 참여자를 만든다. 이 데이터는 세션 생성과 강사 제출의 기반이 된다.

### 사용자 관리와 강사 배정

운영자는 강사를 초대하거나 승인하고, 강사를 수업에 배정한다.

### create-first 세션 생성

운영자는 날짜, 마을, 사업, 수업을 선택해 세션을 만든다. 강사와 참여자가 비어 있어도 세션 생성은 가능하다.

### 운영자 세션 관리

운영자는 이미 만든 세션에 강사를 배정하거나, 기존 참여자를 추가하거나, 새 참여자를 만들고 즉시 세션에 추가한다.

### 강사 제출

강사는 자기에게 배정된 세션만 열 수 있다. snapshot이 1명 이상일 때 출석과 교육일지를 제출할 수 있다.

### 운영자 기록 조회

운영자는 세션별 제출 상태, 최근 제출, 최근 수정, 기록 상세를 조회한다.

## Architecture Commitments

- MVP는 Next.js App Router 기반 단일 웹 앱이다.
- 웹 UI 쓰기 진입점은 server actions를 기본으로 한다.
- 비즈니스 규칙은 `src/server/services`에 둔다.
- 모든 입력은 Zod로 검증한다.
- 모든 기관 소속 데이터 읽기/쓰기는 현재 사용자의 기관 범위 안에서 수행한다.
- 외부 이메일 발송, 배포 자동화, 복잡한 운영 인프라는 1차 local-first MVP 범위 밖이다.
